# Synaplan - Standard Installation (Recommended)
#
# Cloud AI by default, exactly like a self-hosted install (deploy/compose.yaml):
# - No AI weights are downloaded; add one provider key and chat works
# - Whisper audio transcription (tiny model, ~75 MB)
# - Download size: ~3 GB
#
# Usage:
#   docker compose up -d
#
# To add local AI (Ollama + the bge-m3 embedding model, ~1 GB extra):
#   COMPOSE_PROFILES=local-ai docker compose up -d
# The same variable does the same thing in production -- see deploy/README.md.
#
# Lean by design -- core vs optional building blocks
# ---------------------------------------------------
# Core (always):     frontend, backend, worker, db (MariaDB), redis
# On by default,     qdrant (memories / vectors), centrifugo (live support),
#   switchable:      tika (text extraction), whisper (audio transcription)
#                    -> stop one:  docker compose stop <name>
#                       (Synaplan keeps running; the matching feature hides)
#                    -> keep it off across restarts: give it a profile in a
#                       docker-compose.override.yml, e.g.
#                         services: { qdrant: { profiles: [optional] } }
# Opt-in profiles:   local-ai (Ollama + local embedding model, ~1 GB)
#                    office   (Collabora CODE, office files, ~2 GB RAM)
#                    tts      (Piper text-to-speech)
#                    oidc     (Keycloak, SSO test realm)
#                    -> COMPOSE_PROFILES=local-ai,office docker compose up -d
# The boot page at http://localhost:5173 shows which blocks are running.
#
services:
  # ----------------------------------------------------------------------------
  # Startup notes (one-shot) - human-readable guidance for `docker compose up`.
  #
  # `docker compose up -d` detaches immediately and only prints container
  # status lines, so this tiny container carries the "please wait" notes in
  # its logs and reports READY once backend + frontend actually answer:
  #
  #   docker compose logs -f startup-notes
  #
  # With attached `docker compose up` (no -d) the notes appear directly in the
  # terminal. restart: "no" makes it one-shot; every later `up` re-runs it,
  # which then finishes in seconds because the stack is already healthy.
  # ----------------------------------------------------------------------------
  startup-notes:
    image: alpine:3.24@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b
    container_name: synaplan-startup-notes
    command:
      - sh
      - -c
      # Quoted heredoc = no shell expansion in the banner; $$ = compose escape.
      - |
        started=$$(date +%s)
        elapsed() { echo $$(( $$(date +%s) - started )); }
        cat <<'EOF'

        ============================================================
         Synaplan is starting
        ============================================================
         OPEN  http://localhost:5173  RIGHT NOW -- a live status
         screen shows every boot step (database, backend, AI model
         download) and switches to the app automatically.

         The first start takes a few minutes depending on your
         machine and connection. In the background Docker is:
           - building the backend dev image (first run only)
           - installing frontend dependencies (npm ci)
           - running database migrations + seeding

         When everything is ready:
           App      http://localhost:5173
           Login    admin@synaplan.com / admin123
           API      http://localhost:8000  (docs: /api/doc)
           Mail UI  http://localhost:8025  (MailHog)
           DB UI    http://localhost:8082  (phpMyAdmin)

         Chat needs one AI provider key -- the app greets you with
         a first-run setup screen and guides you (free key: Groq).

         LEAN BY DESIGN -- optional building blocks
           Core is small: app + MariaDB + Redis. Everything else is a
           block you switch on when you need it, off when you don't:
             Qdrant      memories / vectors  on   (docker compose stop qdrant)
             Centrifugo  live support        on   (REALTIME_ENABLED=false)
             Tika        text extraction     on   (docker compose stop tika)
             Ollama      local AI            off  (COMPOSE_PROFILES=local-ai)
             Collabora   office files        off  (--profile office)
             TTS         spoken answers      off  (--profile tts)
           Off = Synaplan keeps running, the feature simply stays hidden.
           The status screen lists the live state of every block.

         Follow this readiness check:  docker compose logs -f startup-notes
         Follow the whole stack:       docker compose logs -f
        ============================================================

        EOF
        echo "[startup-notes] waiting for the backend (image build + migrations can take a while on first boot)..."
        i=0
        until wget -q -T 3 -O /dev/null http://backend/api/health 2>/dev/null; do
          i=$$((i + 1))
          if [ $$((i % 6)) -eq 0 ]; then
            echo "[startup-notes] still waiting for the backend... ($$(elapsed)s elapsed -- normal on first boot)"
          fi
          if [ "$$(elapsed)" -ge 1500 ]; then
            echo "[startup-notes] backend still not ready after 25 minutes -- something is probably wrong."
            echo "[startup-notes] check:  docker compose ps   and   docker compose logs backend"
            exit 1
          fi
          sleep 5
        done
        echo "[startup-notes] backend is up (after $$(elapsed)s)"
        echo "[startup-notes] waiting for the frontend dev server (npm ci + schema generation + Vite start)..."
        # The boot-status onboarding page answers on :5173 long before Vite
        # does, so "port answers" is not enough: the app is only ready when
        # the port answers AND the boot page's status feed is gone (the
        # entrypoint stops it right before Vite takes over). Vite answers
        # /boot-status.json too (SPA html fallback, 200), so we must inspect
        # the BODY: only the real feed contains "boot":true.
        while true; do
          # A failed /boot-status.json fetch must NOT count as ready: `! wget | grep`
          # is true when wget itself fails, which used to print READY too early.
          body=$$(wget -q -T 3 -O - http://frontend:5173/boot-status.json 2>/dev/null) || body=""
          if wget -q -T 3 -O /dev/null http://frontend:5173 2>/dev/null \
            && [ -n "$$body" ] \
            && ! printf '%s' "$$body" | grep -q '"boot":true'; then
            break
          fi
          if [ "$$(elapsed)" -ge 1800 ]; then
            echo "[startup-notes] frontend still not ready after 30 minutes -- something is probably wrong."
            echo "[startup-notes] check:  docker compose logs frontend"
            exit 1
          fi
          sleep 5
        done
        cat <<EOF

        ============================================================
         Synaplan is READY (took $$(elapsed)s)

           Open   http://localhost:5173
           Login  admin@synaplan.com / admin123
        ============================================================

        EOF
    restart: "no"
    networks:
      - synaplan-network

  # Vue.js Frontend
  frontend:
    image: node:24@sha256:be23f54a88d34e8824c741b19b91064094f92c1c97b194144bfc8b50d67258e2
    container_name: synaplan-frontend
    working_dir: /app
    entrypoint: ["/usr/local/bin/docker-entrypoint.sh"]
    environment:
      NODE_ENV: development
      BACKEND_URL: http://backend
      # Vite dev-server host check (anti DNS-rebinding). EMPTY by default ->
      # ignored, so the dev server answers to ALL hosts out of the box (works
      # behind any domain / reverse proxy). Set ALLOWED_HOSTS to a
      # comma-separated allow-list to restrict to ONLY those hosts.
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-}
      # Probed by the boot-status onboarding page (see /boot-status mount).
      # The entrypoint blanks the Ollama URL unless the `local-ai` profile is
      # on, so the page shows local AI as an opt-in block instead of probing a
      # container that was never started.
      COMPOSE_PROFILES: ${COMPOSE_PROFILES:-}
      BOOT_STATUS_OLLAMA_URL: http://ollama:11434
      BOOT_STATUS_DB_HOST: db
      # Optional building blocks the boot page lists with a live on/off state
      # (TCP probe per entry). A profile that is not enabled leaves the host
      # name unresolvable, so the block shows as "off" with its enable command.
      BOOT_STATUS_COMPANIONS: qdrant=qdrant:6333,centrifugo=centrifugo:8000,tika=tika:9998,collabora=collabora:9980,tts=tts:10200
    ports:
      - "5173:5173"
    volumes:
      - ./_docker/frontend/docker-entrypoint.sh:/usr/local/bin/docker-entrypoint.sh:ro
      - ./_devextras/frontend/docker-entrypoint.d:/docker-entrypoint.d:ro
      # Boot-status onboarding page: a zero-dependency Node server the entrypoint
      # runs on :5173 BEFORE npm ci, so the browser shows live boot progress
      # instead of a connection error during a cold first start.
      - ./_docker/frontend/boot-status:/boot-status:ro
      # Read-only view of the backend's var/ so the boot-status page can show
      # backend boot milestones (boot-status.json) and the Ollama model download
      # progress (ollama-download.json) written by the backend entrypoint.
      - ./backend/var:/synaplan-backend-var:ro
      - ./frontend:/app
       # Anonymous volume to prevent host node_modules from being mounted (avoids platform-specific binary conflicts)
      - /app/node_modules
    # NO depends_on -- on purpose. The frontend must start IMMEDIATELY so the
    # boot-status onboarding page binds :5173 within seconds of `up -d`, even
    # while the backend image is still building on a fresh checkout. The only
    # real backend dependency is schema generation (20-generate-schemas.sh),
    # which polls the backend API for up to ~15 min and, combined with
    # `restart: unless-stopped`, retries until it succeeds.
    #
    # (History: gating on service_healthy made `up -d` block for the backend's
    # 900s start_period and could strand the frontend in state "Created";
    # service_started fixed that but still delayed the boot page by the whole
    # image build + db health wait. No dependency is both faster and safer.)
    #
    # Vite dev server readiness: the boot-status page answers on :5173 within
    # seconds, so this healthcheck turns healthy early -- it verifies that port
    # 5173 serves SOMETHING useful (boot page first, Vite after the handover).
    # start_period still covers a slow cold boot of the real dev server.
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5173',r=>{r.resume();process.exit(r.statusCode<400?0:1)}).on('error',()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 300s
    restart: unless-stopped
    networks:
      - synaplan-network

  # Widget Build Watcher (dev only)
  # Memory-limited to prevent unbounded growth from Chokidar polling (#515).
  # Container auto-restarts on OOM thanks to restart policy.
  # 6g: the Rolldown-based Vite (>= 8.0.16) peaks above the old 4g cap while
  # rendering chunks (native binary + JS heap), which OOM-killed every build
  # and left dist-widget without widget.js.
  frontend-widgets:
    image: node:24@sha256:be23f54a88d34e8824c741b19b91064094f92c1c97b194144bfc8b50d67258e2
    container_name: synaplan-frontend-widgets
    working_dir: /app
    command: sh -c "npm ci && npm run build:widget -- --watch"
    environment:
      NODE_ENV: development
      NODE_OPTIONS: '--max-old-space-size=4096'
    mem_limit: 6g
    memswap_limit: 6g
    volumes:
      - ./frontend:/app
      # Anonymous volume to prevent host node_modules from being mounted
      - /app/node_modules
    restart: unless-stopped
    networks:
      - synaplan-network

  # Symfony PHP Backend
  # Serves both API and production frontend from frontend/dist
  backend:
    # Docker's embedded resolver (127.0.0.11) MUST be listed first -- otherwise
    # only public DNS is used and service hostnames (db, ollama, ...) never resolve,
    # which puts the backend in a crash loop and prevents the frontend schema step
    # from ever reaching a healthy API.
    dns:
      - 127.0.0.11
      - 8.8.8.8
      - 8.8.4.4
    image: ghcr.io/metadist/synaplan:latest
    # Always build the lightweight `dev` stage locally so the image carries
    # ext-redis (phpredis) without baking the app. The published :latest can be
    # stale; reusing it breaks composer install (symfony/redis-messenger platform
    # req) and the worker's Redis Streams consumer. The dev stage is independent
    # of backend source, so every `up -d` is a cache hit after the first build.
    pull_policy: build
    build:
      context: .
      dockerfile: _docker/backend/Dockerfile
      target: dev
    container_name: synaplan-backend
    # Optional: Load extra config from backend/.env (for API keys, etc.)
    env_file:
      - path: backend/.env
        required: false
    ports:
      - "8000:80"
    volumes:
      - ./backend:/var/www/backend
      # Writable Symfony cache inside the container. Bind-mounted ./backend/var/cache often
      # inherits host UID/GID (e.g. macOS), so www-data cannot write and PHPUnit fails with
      # "Unable to write in the cache directory (/var/www/backend/var/cache/test)".
      - /var/www/backend/var/cache
      - ./frontend/dist:/var/www/frontend:ro
      - ./frontend/dist-widget:/var/www/widgets
      - ./plugins:/plugins:ro
      - ./_docker/backend/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./_docker/backend/docker-entrypoint.sh:/usr/local/bin/docker-entrypoint.sh:ro
      - ./_docker/backend/lib:/usr/local/bin/lib:ro
      - whisper_models:/var/www/backend/var/whisper:ro
      - ./_devextras/backend/docker-entrypoint.d:/docker-entrypoint.d:ro
    environment:
      APP_ENV: dev
      # Demo user fixtures. Set SEED_DEMO_DATA=false to boot an EMPTY database
      # and walk through the first-run setup wizard at /setup, the way a
      # production install sees it. Production never loads fixtures at all.
      SEED_DEMO_DATA: ${SEED_DEMO_DATA:-true}
      # Local Docker Nextcloud (http://nextcloud) — ignored unless APP_ENV=dev.
      DAV_ALLOW_INSECURE_LOCAL: "1"
      # APP_SECRET loaded from backend/.env (env_file above)
      APP_URL: http://localhost:8000
      PLUGINS_DIR: /plugins
      FRONTEND_URL: http://localhost:5173
      DATABASE_WRITE_URL: mysql://synaplan_user:synaplan_password@db:3306/synaplan?serverVersion=mariadb-12.2.2&charset=utf8mb4
      DATABASE_READ_URL: mysql://synaplan_user:synaplan_password@db:3306/synaplan?serverVersion=mariadb-12.2.2&charset=utf8mb4
      # Empty until the `local-ai` profile sets it in the command below. An
      # empty URL means "no local AI here", which the health probes report as
      # not-configured rather than as an outage (#1704).
      # The command wrapper exports it for the app process only, so a
      # `docker compose exec backend php bin/console …` session does NOT see
      # it. Put OLLAMA_BASE_URL=http://ollama:11434 next to
      # COMPOSE_PROFILES=local-ai in ./.env when you need console commands to
      # reach the local models too.
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-}
      COMPOSE_PROFILES: ${COMPOSE_PROFILES:-}
      QDRANT_URL: http://qdrant:6333
      TRITON_SERVER_URL: ${TRITON_SERVER_URL:-}
      TIKA_BASE_URL: http://tika:9998
      # Optional Docling sidecar (profile `docling`). The URL is always set so
      # enabling the profile needs no extra env; an empty .env value keeps it
      # off outside compose. A down sidecar is unavailable, never a failed upload.
      DOCLING_BASE_URL: ${DOCLING_BASE_URL:-http://docling:5001}
      DOCLING_TIMEOUT_MS: ${DOCLING_TIMEOUT_MS:-120000}
      SEARXNG_BASE_URL: ${SEARXNG_BASE_URL:-http://searxng:8080}
      # Collabora CODE convert-to. Local default is the compose service name.
      # Override with OFFICE_CONVERT_URL for an external CODE, or set
      # OFFICE_CONVERT_URL=disabled to turn the engine off. The sidecar still
      # needs `--profile office` (it is not started by a plain `up -d`).
      OFFICE_CONVERT_URL: ${OFFICE_CONVERT_URL:-http://collabora:9980}
      OFFICE_CONVERT_TIMEOUT_MS: ${OFFICE_CONVERT_TIMEOUT_MS:-60000}
      AI_DEFAULT_PROVIDER: ollama
      # Token Configuration
      TOKEN_SECRET: 'change_me_in_production_use_openssl_rand_hex_32'
      LOCK_DSN: ${LOCK_DSN:-redis://redis:6379}
      # Redis (general-purpose: cache, locks, rate-limiter, realtime fan-out)
      # Override REDIS_DSN for production (e.g. redis://redis-cluster.internal:6379).
      REDIS_DSN: ${REDIS_DSN:-redis://redis:6379}
      # Realtime gateway (Centrifugo). REALTIME_ENABLED toggles WS publishing
      # at runtime; there is no SSE fallback after the realtime cleanup phase.
      REALTIME_ENABLED: ${REALTIME_ENABLED:-true}
      REALTIME_API_URL: ${REALTIME_API_URL:-http://centrifugo:8000/api}
      REALTIME_API_KEY: ${REALTIME_API_KEY:-changeme_centrifugo_api_key}
      REALTIME_TOKEN_SECRET: ${REALTIME_TOKEN_SECRET:-changeme_centrifugo_token_secret}
      # WebSocket URL exposed to browser clients via /api/v1/config/runtime.
      # Empty string = same-origin (Caddy reverse-proxies /connection/* to centrifugo).
      REALTIME_PUBLIC_WS_URL: ${REALTIME_PUBLIC_WS_URL:-}
      # Mailer (Mailhog for development)
      MAILER_DSN: smtp://mailhog:1025
      # External API Keys loaded from backend/.env via env_file
      # WhatsApp Business API - Fully Dynamic Multi-Number Support
      WHATSAPP_ACCESS_TOKEN: ${WHATSAPP_ACCESS_TOKEN:-}
      WHATSAPP_WEBHOOK_VERIFY_TOKEN: ${WHATSAPP_WEBHOOK_VERIFY_TOKEN:-}
      WHATSAPP_ENABLED: ${WHATSAPP_ENABLED:-false}
      # AI Provider API Keys (loaded from backend/.env via env_file above)
      # GROQ_API_KEY is automatically loaded from backend/.env
      AUTO_DOWNLOAD_MODELS: "false"
      ENABLE_LOCAL_GPT_OSS: ${ENABLE_LOCAL_GPT_OSS:-false}
      # reCAPTCHA (optional - disabled by default)
      RECAPTCHA_SITE_KEY: ${RECAPTCHA_SITE_KEY:-}
      RECAPTCHA_SECRET_KEY: ${RECAPTCHA_SECRET_KEY:-}
      RECAPTCHA_ENABLED: ${RECAPTCHA_ENABLED:-false}
      # Stripe Payment Integration - loaded from backend/.env via env_file
      # (STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_*)
      # OAuth Social Login Providers (loaded from backend/.env via env_file)
      # Whisper.cpp Service (Audio Transcription)
      WHISPER_BINARY: /usr/local/bin/whisper
      WHISPER_MODELS_PATH: /var/www/backend/var/whisper
      WHISPER_DEFAULT_MODEL: tiny
      WHISPER_ENABLED: "true"
      FFMPEG_BINARY: /usr/bin/ffmpeg
      # Piper TTS (optional). Default reaches a host-bound synaplan-tts
      # (`docker compose --profile tts` or the standalone repo on :10200).
      # Image: ghcr.io/metadist/synaplan-tts — 4 baked voices (en/de/es/tr).
      SYNAPLAN_TTS_URL: ${SYNAPLAN_TTS_URL:-http://host.docker.internal:10200}
      # Raise PHP's `max_file_uploads` (base-php default is 20). The Files /
      # RAG manager allows selecting any number of files but PHP silently
      # truncates $_FILES at this limit, leaving the user with no error and
      # missing uploads. 100 here, combined with the explicit overflow guard
      # in FileController + client-side batching in filesService.ts, makes
      # the failure mode loud and the happy path support up to ~100 files
      # per request without batching. Override via PHP_MAX_FILE_UPLOADS in
      # your env if you need more / less. Honored by base-php's
      # 99-synaplan-env.ini render step.
      PHP_MAX_FILE_UPLOADS: "100"
    # Local AI is opt-in: `COMPOSE_PROFILES=local-ai docker compose up -d`.
    # Compose cannot make one service's environment depend on an enabled
    # profile, so the app roles derive it here -- the same wrapper, and the
    # same list-ELEMENT match, that deploy/compose.yaml uses in production.
    # Matching the whole value instead would let "local-ai,office" start
    # Ollama while leaving the app pointed at no local AI at all.
    entrypoint: ["/bin/bash", "-ec"]
    command:
      - |
        case ",$${COMPOSE_PROFILES:-}," in
          *,local-ai,*)
            export OLLAMA_BASE_URL=http://ollama:11434
            export AUTO_DOWNLOAD_MODELS=true
            ;;
        esac
        exec /usr/local/bin/docker-entrypoint.sh
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      centrifugo:
        condition: service_started
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
      # Platform-compatible alias so the same `QDRANT_URL=http://docker-host:6333`
      # that production uses also resolves in dev. Lets the dev backend talk to
      # either the synaplan-memories Qdrant (if running via its own compose) or
      # any other host-bound service without changing backend/.env.
      - "docker-host:host-gateway"
    # Generous start_period so a slow first boot never blocks the frontend.
    #
    # The frontend `depends_on: backend: service_healthy` -- it is only created
    # once this probe passes. On a FRESH checkout the very first `up -d` runs
    # the backend boot (migrations + app:seed + cache warm) WHILE the machine is
    # saturated pulling images, building the dev image, and downloading multi-GB
    # Ollama models (bge-m3 + gpt-oss:20b). On a loaded host the first healthy
    # /api/health routinely lands well past 10 minutes.
    #
    # Docker's healthcheck rule: while inside `start_period`, a FAILING probe
    # keeps the container in state "starting" (NOT "unhealthy"), so
    # `depends_on: service_healthy` keeps WAITING instead of aborting the
    # frontend with "dependency failed to start: container synaplan-backend is
    # unhealthy". The moment /api/health answers 200 the container flips to
    # "healthy" -- the long start_period is only an upper bound, it does not delay
    # a backend that boots quickly. (Model downloads themselves are backgrounded
    # in the entrypoint and never block FrankenPHP from binding port 80.)
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/api/health"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 900s
    networks:
      - synaplan-network

  # Symfony Messenger Worker
  #
  # Consumes the three async transports defined in
  # backend/config/packages/messenger.yaml so async jobs (chat
  # ProcessMessageCommand, embedding ReVectorize, plugin install,
  # widget crawling, ...) actually run in local dev. Without this the
  # Doctrine queue (`messenger_messages` table) just grows forever and
  # the UI hangs on any "queued / running in background" flow.
  #
  # Runs in `APP_ENV=prod` on purpose: the dev-mode TraceableEventDispatcher
  # has a long-standing bug where `messenger:consume` triggers
  #   Warning: Undefined array key "Symfony\Component\Messenger\Event\WorkerRunningEvent"
  # which kills the consumer on the first message. The web container stays
  # in `dev` so the profiler / debug toolbar still work for HTTP traffic.
  #
  # `--time-limit=3600` + `--memory-limit=512M` cooperate with `restart:
  # unless-stopped` to recycle the worker hourly, which is the standard
  # Symfony pattern to bound Doctrine EM growth on long-lived consumers.
  worker:
    image: ghcr.io/metadist/synaplan:latest
    # Build the same lightweight `dev` stage as the backend (base + phpredis).
    # The worker bind-mounts ./backend and consumes Redis Streams via
    # messenger:consume, which needs phpredis at runtime; the dev stage is
    # source-independent so `up -d` stays a cache hit after the first build.
    pull_policy: build
    build:
      context: .
      dockerfile: _docker/backend/Dockerfile
      target: dev
    container_name: synaplan-worker
    env_file:
      - path: backend/.env
        required: false
    volumes:
      - ./backend:/var/www/backend
      - /var/www/backend/var/cache
      - ./plugins:/plugins:ro
      - whisper_models:/var/www/backend/var/whisper:ro
    environment:
      # Worker MUST run in the same APP_ENV as the backend, otherwise the two
      # halves of the system end up in different Redis namespaces
      # (RedisService prefixes every key with "synaplan:{env}:"). With backend
      # in dev and worker in prod, the worker happily consumed
      # AdvanceMediaJobCommand messages but every findByKey() lookup missed
      # because the job lived under synaplan:dev:mediajob:* and the worker
      # looked under synaplan:prod:mediajob:* -- silent "job not found",
      # bubble stuck in queued forever. The synaplan-platform compose
      # overrides both to prod for production.
      APP_ENV: ${APP_ENV:-dev}
      APP_DEBUG: "0"
      DAV_ALLOW_INSECURE_LOCAL: "1"
      APP_URL: http://backend
      PLUGINS_DIR: /plugins
      DATABASE_WRITE_URL: mysql://synaplan_user:synaplan_password@db:3306/synaplan?serverVersion=mariadb-12.2.2&charset=utf8mb4
      DATABASE_READ_URL: mysql://synaplan_user:synaplan_password@db:3306/synaplan?serverVersion=mariadb-12.2.2&charset=utf8mb4
      # Empty until the `local-ai` profile sets it in the command below.
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-}
      COMPOSE_PROFILES: ${COMPOSE_PROFILES:-}
      QDRANT_URL: http://qdrant:6333
      TIKA_BASE_URL: http://tika:9998
      DOCLING_BASE_URL: ${DOCLING_BASE_URL:-http://docling:5001}
      DOCLING_TIMEOUT_MS: ${DOCLING_TIMEOUT_MS:-120000}
      SEARXNG_BASE_URL: ${SEARXNG_BASE_URL:-http://searxng:8080}
      OFFICE_CONVERT_URL: ${OFFICE_CONVERT_URL:-http://collabora:9980}
      OFFICE_CONVERT_TIMEOUT_MS: ${OFFICE_CONVERT_TIMEOUT_MS:-60000}
      AI_DEFAULT_PROVIDER: ollama
      TOKEN_SECRET: 'change_me_in_production_use_openssl_rand_hex_32'
      LOCK_DSN: ${LOCK_DSN:-redis://redis:6379}
      # Redis (general-purpose: cache, locks, rate-limiter, Messenger Redis Streams
      # transport). The worker consumes async_ai_high/async_extract/async_index,
      # all of which use the Redis transport, so REDIS_DSN is mandatory here.
      REDIS_DSN: ${REDIS_DSN:-redis://redis:6379}
      # Realtime gateway (Centrifugo) -- MUST resolve to the same values as the
      # backend and centrifugo services, which read them from the same compose
      # interpolation. Without these entries the worker falls through to
      # `env_file: backend/.env`; any REALTIME_API_KEY set there diverges from
      # centrifugo's, every publish is rejected with 401, and async media jobs
      # complete on the server while the browser bubble stays "Working..."
      # forever because no progress event ever arrives.
      REALTIME_ENABLED: ${REALTIME_ENABLED:-true}
      REALTIME_API_URL: ${REALTIME_API_URL:-http://centrifugo:8000/api}
      REALTIME_API_KEY: ${REALTIME_API_KEY:-changeme_centrifugo_api_key}
      REALTIME_TOKEN_SECRET: ${REALTIME_TOKEN_SECRET:-changeme_centrifugo_token_secret}
      MAILER_DSN: smtp://mailhog:1025
      AUTO_DOWNLOAD_MODELS: "false"
      ENABLE_LOCAL_GPT_OSS: ${ENABLE_LOCAL_GPT_OSS:-false}
      WHISPER_BINARY: /usr/local/bin/whisper
      WHISPER_MODELS_PATH: /var/www/backend/var/whisper
      WHISPER_DEFAULT_MODEL: tiny
      WHISPER_ENABLED: "true"
      FFMPEG_BINARY: /usr/bin/ffmpeg
      SYNAPLAN_TTS_URL: ${SYNAPLAN_TTS_URL:-http://host.docker.internal:10200}
    entrypoint: ["bash", "-c"]
    command:
      - |
        set -u

        # Local AI is opt-in and must resolve to the SAME answer as the backend
        # role, otherwise a queued embedding job would look for an Ollama the
        # web container knows nothing about. Same wrapper as deploy/compose.yaml.
        case ",$${COMPOSE_PROFILES:-}," in
          *,local-ai,*)
            export OLLAMA_BASE_URL=http://ollama:11434
            ;;
        esac

        # Resolve the env once so cache:warmup, dbal:run-sql, and
        # messenger:consume all agree. APP_ENV is set in the service env
        # block above (default: dev for local; prod overrides via compose).
        ENV="$${APP_ENV:-dev}"
        echo "[worker] APP_ENV=$$ENV"

        # Fail-fast diagnostic: a broken bind-mount (the codebase missing from
        # /var/www/backend, leaving only the anonymous var/cache volume) used
        # to make the DB-wait loop below silently hang forever -- every retry
        # hit "Could not open input file: bin/console" but the error was
        # suppressed, so messenger:consume never started and every queued
        # AdvanceMediaJobCommand sat unprocessed. Crash loudly instead.
        if [ ! -f bin/console ]; then
          echo "[worker] FATAL: bin/console missing under $$(pwd)." >&2
          echo "[worker] The ./backend bind-mount is broken; restart the worker" >&2
          echo "[worker] container or rebuild the dev stack."                   >&2
          exit 64
        fi

        # Clear the cache BEFORE any console command runs. The cache lives
        # under var/cache/$$ENV/ on the bind-mounted backend volume, so it
        # otherwise survives across container rebuilds and pins the compiled
        # DI container to whatever code revision happened to boot it first.
        #
        # If we ran a console command first (e.g. dbal:run-sql), Symfony would
        # try to load the stale compiled container and crash on any class that
        # was removed in the new code -- for example after the Doctrine DBAL
        # upgrade the cached container references the removed
        # `Doctrine\DBAL\Schema\LegacySchemaManagerFactory` and the worker
        # deadlocks on its own DB readiness loop (issue #882). Same class of
        # symptom as ArgumentCountError / "Too few arguments to function
        # ::__construct()" after service constructor changes. Clearing on boot
        # is a few seconds and matches what every Symfony deploy does anyway.
        echo "[worker] clearing $$ENV cache..."
        rm -rf "var/cache/$$ENV"
        if ! php bin/console --env="$$ENV" cache:warmup; then
          echo "[worker] FATAL: cache:warmup failed -- refusing to start." >&2
          exit 65
        fi

        # Wait for the backend container to finish migrations, otherwise the
        # first consume cycle races against schema and crashes with
        # "Table doctrine_migration_versions doesn't exist".
        #
        # Bounded: 60 attempts x 3s = ~3 min ceiling. After that we exit so the
        # `restart: unless-stopped` policy + `docker compose ps` surface the
        # problem instead of letting the worker pretend it is healthy forever.
        attempt=0
        max_attempts=60
        while ! php bin/console --env="$$ENV" dbal:run-sql 'SELECT 1' >/dev/null 2>&1; do
          attempt=$$((attempt + 1))
          if [ "$$attempt" -ge "$$max_attempts" ]; then
            echo "[worker] FATAL: db schema not ready after $$((max_attempts * 3))s." >&2
            php bin/console --env="$$ENV" dbal:run-sql 'SELECT 1' >&2 || true
            exit 66
          fi
          # Echo the underlying error every 10 attempts so a real problem
          # (auth failure, wrong host, broken vendor/) is visible.
          if [ $$((attempt % 10)) -eq 1 ]; then
            echo "[worker] waiting for db schema... (attempt $$attempt/$$max_attempts)" >&2
            php bin/console --env="$$ENV" dbal:run-sql 'SELECT 1' >&2 || true
          fi
          sleep 3
        done

        # Liveness sentinel -- touched on boot, refreshed by the consumer (see
        # messenger middleware in production for periodic refresh). The
        # healthcheck below treats an old sentinel as unhealthy.
        mkdir -p var/worker
        date +%s > var/worker/last_alive

        echo "[worker] db + cache ready, starting messenger:consume (env=$$ENV)"
        exec php bin/console --env="$$ENV" messenger:consume \
          async_ai_high async_extract async_index \
          --time-limit=3600 --memory-limit=512M -v
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      backend:
        condition: service_started
    restart: unless-stopped
    # Worker liveness: probe that messenger:consume is actually running.
    # Surfaces the silent-hang class of failure that took us hours to diagnose
    # (broken bind mount -> infinite "waiting for db schema..." loop with no
    # consumer ever starting; or env-prefix mismatch -> consumer running but
    # every job a no-op). A missing process = unhealthy = visible in
    # `docker compose ps`.
    #
    # We scan /proc/*/cmdline rather than using pgrep/ps: the base image is
    # minimal and ships NEITHER procps nor pgrep, so a pgrep-based check would
    # report the worker unhealthy forever even while it consumes happily. grep
    # is always present; cmdline args are NUL-separated, which grep matches as a
    # plain substring.
    healthcheck:
      test:
        - "CMD-SHELL"
        - "grep -lq 'messenger:consume' /proc/[0-9]*/cmdline 2>/dev/null"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 120s
    extra_hosts:
      - "host.docker.internal:host-gateway"
      - "docker-host:host-gateway"
    networks:
      - synaplan-network

  # MariaDB 11.8 Database
  db:
    image: mariadb:12.3.3@sha256:dd9b303aed4f4890ed09f766d8ca9ddfd176c0c6f6267feff53b3192ec65a979
    container_name: synaplan-db
    command: --max-allowed-packet=256M --innodb-buffer-pool-size=512M
    environment:
      MYSQL_DATABASE: synaplan
      MYSQL_USER: synaplan_user
      MYSQL_PASSWORD: synaplan_password
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./_devextras/db-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mariadb", "-h", "localhost", "-u", "root", "-proot_password", "-e", "SELECT 1;"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped
    networks:
      - synaplan-network

  # ----------------------------------------------------------------------------
  # Redis - General-purpose in-memory data store.
  #
  # Used by:
  #   * Symfony cache.adapter.redis (pools: app, widget_events, model_config, ...)
  #   * Symfony Lock component (LOCK_DSN=redis://redis:6379)
  #   * Symfony Rate-Limiter storage (per-user quotas)
  #   * Centrifugo Redis engine (cross-node WebSocket pub/sub)
  #   * Symfony Messenger transports (Redis Streams, consumed by the worker)
  #   * Native PHP sessions (RedisSessionHandler)
  #
  # Production note: replace with managed/clustered Redis (e.g. one shared
  # instance for all 3 web nodes). Keep --maxmemory and eviction policy in
  # mind once persistent caches grow beyond a few hundred MB.
  # ----------------------------------------------------------------------------
  redis:
    # Pinned to Redis 7.4: Centrifugo v6.4.0 (rueidis/RESP3) reliably receives
    # client publications from 7.x, whereas Redis 8.x dropped pub/sub fan-out to
    # the Centrifugo node (control messages arrived, publications did not),
    # which silently broke realtime delivery to dashboard + widget clients.
    image: redis:7.4-alpine@sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf
    container_name: synaplan-redis
    command: >-
      redis-server
        --appendonly no
        --save ""
        --maxmemory 256mb
        --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped
    networks:
      - synaplan-network

  # ----------------------------------------------------------------------------
  # Centrifugo - WebSocket / real-time gateway.
  #
  # One instance per web node in production. All instances share state through
  # the Redis engine, so a publish on node A reaches subscribers on node B.
  # Caddy/FrankenPHP reverse-proxies `/connection/*` and `/centrifugo/api/*`
  # so browsers connect to the same origin (Cloudflare-friendly, no CORS).
  # Authentication uses short-lived JWTs issued by the PHP backend.
  # ----------------------------------------------------------------------------
  centrifugo:
    image: centrifugo/centrifugo:v6.9.4@sha256:78b2fcd11e69f7cd82072924db99865be2beba4a03087d8ce0f5008ae357abe4
    container_name: synaplan-centrifugo
    command: ["centrifugo", "-c", "/centrifugo/config.json"]
    volumes:
      - ./_docker/centrifugo/config.json:/centrifugo/config.json:ro
    environment:
      # Real configuration lives in config.json; these vars override secrets so
      # production deployments don't ship credentials in the repo. Centrifugo v6
      # nests config under typed sections, so env var names mirror the JSON path
      # (CENTRIFUGO_<SECTION>_<FIELD>, with `_` for nested levels).
      CENTRIFUGO_CLIENT_TOKEN_HMAC_SECRET_KEY: ${REALTIME_TOKEN_SECRET:-changeme_centrifugo_token_secret}
      CENTRIFUGO_HTTP_API_KEY: ${REALTIME_API_KEY:-changeme_centrifugo_api_key}
      CENTRIFUGO_ADMIN_PASSWORD: ${REALTIME_ADMIN_PASSWORD:-changeme_admin}
      CENTRIFUGO_ADMIN_SECRET: ${REALTIME_ADMIN_SECRET:-changeme_admin_secret}
      # Space-separated list of origins allowed to upgrade to WebSocket.
      # Leave default for dev; in production set to your domain(s).
      CENTRIFUGO_CLIENT_ALLOWED_ORIGINS: ${REALTIME_ALLOWED_ORIGINS:-*}
      # Redis engine address (cross-node pub/sub). Override REALTIME_REDIS_ADDRESS
      # in production to point at the shared/managed Redis, e.g.
      # redis://redis-prod.internal:6379/3. Keep the dedicated logical DB suffix
      # (/3) -- it isolates Centrifugo keys from Symfony cache/lock/messenger which
      # use /0 via REDIS_DSN. (For Redis Cluster use a host without a DB suffix.)
      CENTRIFUGO_ENGINE_REDIS_ADDRESS: ${REALTIME_REDIS_ADDRESS:-redis://redis:6379/3}
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - synaplan-network

  # Ollama - Local AI Models (GPU-accelerated if NVIDIA available)
  #
  # Opt-in: `COMPOSE_PROFILES=local-ai docker compose up -d`. Off by default so
  # a fresh clone reaches a working app without pulling ~1 GB of weights; the
  # app roles derive OLLAMA_BASE_URL from the same profile (see backend above).
  ollama:
    profiles: [local-ai]
    image: ollama/ollama:latest@sha256:684d8674b4315fa18f4f0e973a118ec2652ed96f67563277839985175858e0ba
    container_name: synaplan-ollama
    ports:
      - "11435:11434"
    volumes:
      - ollama_data:/root/.ollama
    environment:
      OLLAMA_HOST: 0.0.0.0
    # GPU support should be configured via an override file if needed
    # Healthcheck uses bash /dev/tcp + HTTP probe (the ollama/ollama image has
    # no curl/wget/nc). Measures the round-trip and treats anything slower than
    # 3000 ms as not-yet-ready -- protects against the warm-up window where the
    # API socket is open but the runtime is still loading models.
    healthcheck:
      test:
        - "CMD"
        - "bash"
        - "-c"
        - |
          T0=$$EPOCHREALTIME
          exec 3<>/dev/tcp/localhost/11434 || exit 1
          printf 'GET /api/tags HTTP/1.0\r\nHost: localhost\r\nConnection: close\r\n\r\n' >&3
          read -r status <&3
          ms=$$(awk -v a="$$T0" -v b="$$EPOCHREALTIME" 'BEGIN{printf "%.0f",(b-a)*1000}')
          echo "ollama probe: $${status%$$'\r'} ($${ms} ms)"
          [[ "$$status" == *"200 OK"* ]] || exit 1
          [ "$$ms" -lt 3000 ] || exit 1
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s
    restart: unless-stopped
    networks:
      - synaplan-network

  # Apache Tika - Document Text Extraction
  tika:
    image: apache/tika:3.3.1.0@sha256:90b7fa1dc018434075fce9e1d9b88b1e3d0ea6979d0cf86e116c79a8073ae973
    container_name: synaplan-tika
    command: -h 0.0.0.0 -p 9998
    ports:
      - "9999:9998"
    # Healthcheck uses bash /dev/tcp + HTTP probe (the apache/tika image has no
    # curl/wget). Measures the round-trip on /version and treats anything
    # slower than 3000 ms as not-yet-ready -- Tika's JVM warm-up can keep the
    # port open long before the parser registry is actually responsive.
    healthcheck:
      test:
        - "CMD"
        - "bash"
        - "-c"
        - |
          T0=$$EPOCHREALTIME
          exec 3<>/dev/tcp/localhost/9998 || exit 1
          printf 'GET /version HTTP/1.0\r\nHost: localhost\r\nConnection: close\r\n\r\n' >&3
          read -r status <&3
          ms=$$(awk -v a="$$T0" -v b="$$EPOCHREALTIME" 'BEGIN{printf "%.0f",(b-a)*1000}')
          echo "tika probe: $${status%$$'\r'} ($${ms} ms)"
          [[ "$$status" == *"200 OK"* ]] || exit 1
          [ "$$ms" -lt 3000 ] || exit 1
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped
    networks:
      - synaplan-network

  # Optional Docling document extraction — https://github.com/docling-project/docling-serve
  # CPU image (~4.4 GB) so the sidecar runs without a GPU. Not started by default.
  # Enable with:  docker compose --profile docling up -d
  # No mem_limit in dev (cgroup limits break some sandboxes; see frontend-widgets).
  # Ask-first Docker change: recorded in _devextras/planning/202609_ai_plugs/02_sprint_2_docling.md
  docling:
    image: ghcr.io/docling-project/docling-serve-cpu:v1.32.0@sha256:576fc2074ac77bcfbf3fe27633aa0dd89b452a170b2cd31689c8751e94d60f7a
    container_name: synaplan-docling
    profiles: [docling]
    restart: unless-stopped
    environment:
      DOCLING_SERVE_ENABLE_UI: "0"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5001/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    networks:
      - synaplan-network

  # Optional SearXNG meta search — enable with: docker compose --profile searxng up -d
  # No published host port (JSON API is only on the compose network). No mem_limit
  # (cgroup limits break some sandboxes). Pin includes the multi-arch digest.
  searxng:
    image: searxng/searxng:2026.8.22-9fea41204@sha256:11a9b34cdc0b1ec2b991470a2762ecb5a1a531898289fb51dcd015260450729e
    container_name: synaplan-searxng
    profiles: [searxng]
    restart: unless-stopped
    volumes:
      - ./_devextras/searxng/settings.yml:/etc/searxng/settings.yml:ro
    environment:
      SEARXNG_BASE_URL: http://searxng:8080/
      SEARXNG_SECRET: ${SEARXNG_SECRET:-synaplan-dev-searxng-secret-change-me}
    networks:
      - synaplan-network

  # Whisper Models Download (runs once)
  whisper-models:
    image: alpine:3.24@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b
    container_name: synaplan-whisper-models
    command: >
      sh -c '
        apk add --no-cache curl ca-certificates &&
        mkdir -p /models &&
        if [ ! -f "/models/ggml-tiny.bin" ]; then
          echo "Downloading Whisper tiny model..." &&
          curl -fL --retry 3 --retry-delay 5 --max-time 600 \
            -o "/models/ggml-tiny.bin" \
            "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin" &&
          chmod 644 /models/*.bin &&
          echo "Whisper model downloaded successfully"
        else
          echo "Whisper model already exists, skipping download"
        fi
      '
    volumes:
      - whisper_models:/models
    restart: "no"
    networks:
      - synaplan-network

  # phpMyAdmin (optional - for DB management)
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest@sha256:42a200db07b4e70fbf32c594ad4521cf16399b8e54bbb5adceae98e7566dfbeb
    container_name: synaplan-phpmyadmin
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_USER: synaplan_user
      PMA_PASSWORD: synaplan_password
    ports:
      - "8082:80"
    depends_on:
      - db
    restart: unless-stopped
    networks:
      - synaplan-network

  # MailHog (SMTP testing)
  mailhog:
    image: mailhog/mailhog:latest@sha256:8d76a3d4ffa32a3661311944007a415332c4bb855657f4f6c57996405c009bea
    container_name: synaplan-mailhog
    ports:
      - "1025:1025"  # SMTP
      - "8025:8025"  # Web UI
    restart: unless-stopped
    networks:
      - synaplan-network

  # Generates a self-signed TLS certificate for Keycloak HTTPS (runs once)
  keycloak_certs:
    image: alpine:3.24@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b
    container_name: synaplan-keycloak-certs
    command: sh -c 'if [ -f /certs/cert.pem ]; then echo "Keycloak TLS cert exists"; exit 0; fi && apk add --no-cache openssl && openssl req -x509 -newkey rsa:2048 -keyout /certs/key.pem -out /certs/cert.pem -days 3650 -nodes -subj "/CN=host.docker.internal" -addext "subjectAltName=DNS:host.docker.internal,DNS:keycloak,DNS:localhost" && chmod 644 /certs/*.pem && echo "Keycloak TLS cert generated"'
    volumes:
      - keycloak_certs:/certs
    profiles: [oidc]
    restart: "no"
    networks:
      - synaplan-network

  # Keycloak OIDC Identity Provider (optional - activated with --profile oidc)
  keycloak:
    image: quay.io/keycloak/keycloak:26.7@sha256:ff4257d0d64efbe99ed1ddfaf07765cc3c36dc7518bf8324d41961327f441c54
    container_name: synaplan-keycloak
    depends_on:
      keycloak_certs:
        condition: service_completed_successfully
    command: start-dev
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HEALTH_ENABLED: "true"
      KC_FEATURES: token-exchange:v1,admin-fine-grained-authz:v1
      KC_HOSTNAME: ${KC_HOSTNAME:-https://host.docker.internal:8443}
      KC_HOSTNAME_BACKCHANNEL_DYNAMIC: "true"
      KC_HOSTNAME_STRICT: "false"
      KC_HTTPS_CERTIFICATE_FILE: /opt/keycloak/certs/cert.pem
      KC_HTTPS_CERTIFICATE_KEY_FILE: /opt/keycloak/certs/key.pem
    ports:
      - "8080:8080"
      - "8443:8443"
    volumes:
      - keycloak_certs:/opt/keycloak/certs:ro
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 60s
    profiles: [oidc]
    restart: "no"
    networks:
      - synaplan-network

  keycloak_setup:
    image: quay.io/keycloak/keycloak:26.7@sha256:ff4257d0d64efbe99ed1ddfaf07765cc3c36dc7518bf8324d41961327f441c54
    container_name: synaplan-keycloak-setup
    depends_on:
      keycloak:
        condition: service_healthy
    entrypoint: ["/bin/bash", "/setup/setup.sh"]
    environment:
      KC_SERVER: http://keycloak:8080
      KC_CALLBACK: http://localhost:8000/api/v1/auth/keycloak/callback
      KC_ORIGIN: http://localhost:8000
      KC_CLIENT_ID: ${OIDC_CLIENT_ID:-synaplan-app}
      KC_CLIENT_SECRET: ${OIDC_CLIENT_SECRET:-test-oidc-secret}
    volumes:
      - ./_docker/keycloak/setup.sh:/setup/setup.sh:ro
    profiles: [oidc]
    restart: "no"
    networks:
      - synaplan-network

  # Optional Collabora CODE — LibreOffice convert-to over HTTP.
  # Not started by default (keeps ~2 GB RAM off the core stack). Enable with:
  #   docker compose --profile office up -d
  # Docs: docs/DEVELOPMENT.md ("Office conversion")
  # No published port: the backend talks to collabora:9980 on the compose network.
  collabora:
    image: collabora/code:26.04.3.2.1@sha256:379b8f1fc955dd6d01ba24adf61d1b177048ddaae179ae8c1e6a6342daccb282
    container_name: synaplan-collabora
    profiles: [office]
    restart: unless-stopped
    mem_limit: 2g
    environment:
      extra_params: --o:ssl.enable=false --o:ssl.termination=false --o:num_prespawn_children=2
    # CODE 26.04 is distroless: no bash, so the /dev/tcp probe of 25.04 cannot
    # run here. coolwsd --probe is the upstream check (it is what the image's
    # own HEALTHCHECK runs) and needs the exec form — a plain string would go
    # through CMD-SHELL and fail for the same reason.
    healthcheck:
      test: ["CMD", "/usr/bin/coolwsd", "--probe", "--use-env-vars"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    networks:
      - synaplan-network

  # Optional Piper TTS — four baked voices (en, de, es, tr).
  # Not started by default (keeps the core stack small). Enable with:
  #   docker compose --profile tts up -d
  # Image: https://github.com/metadist/synaplan-tts/pkgs/container/synaplan-tts
  # Docs:  https://docs.synaplan.com/index.php/tts
  # Extra voices: mount more .onnx + .onnx.json into /voices-extra (see the
  # synaplan-tts README). The UI locale / detected reply language selects
  # the matching baked voice automatically.
  tts:
    image: ghcr.io/metadist/synaplan-tts:latest@sha256:3c8ed60619be4ae1913e64ac35c28aa9b48da66e0e5006c972b91d0a5d278bb2
    container_name: synaplan-tts
    profiles: [tts]
    restart: unless-stopped
    ports:
      - "127.0.0.1:10200:10200"
    volumes:
      - tts_extra_voices:/voices-extra
    environment:
      VOICES_DIR: /voices
      EXTRA_VOICES_DIR: /voices-extra
      DEFAULT_VOICE: en_US-lessac-medium
    networks:
      - synaplan-network

  # Qdrant Vector Database (memories + document vectors)
  qdrant:
    image: qdrant/qdrant:v1.19.1@sha256:12364fe851b9f17356fc88189fc06d1b521262e04659ec7345975b00c9246a10
    container_name: synaplan-qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped
    networks:
      - synaplan-network

volumes:
  mysql_data:
  ollama_data:
  whisper_models:
  qdrant_storage:
  keycloak_certs:
  tts_extra_voices:

networks:
  synaplan-network:
    driver: bridge

